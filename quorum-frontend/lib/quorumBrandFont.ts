import { Montserrat } from "next/font/google";

/** Tipografía de marca (wordmark) — geométrica, cercana al manual Quorum */
export const quorumBrandFont = Montserrat({
  subsets: ["latin"],
  weight: ["600", "700"],
});
